<%= form_for(@phrases_subject) do |f| %>
  <% if @phrases_subject.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@phrases_subject.errors.count, "error") %> prohibited this phrases_subject from being saved:</h2>

      <ul>
      <% @phrases_subject.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :phrase_id %><br>
    <%= f.number_field :phrase_id %>
  </div>
  <div class="field">
    <%= f.label :subject_id %><br>
    <%= f.number_field :subject_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
