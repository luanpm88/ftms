<%= form_for(@related_contact) do |f| %>
  <% if @related_contact.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@related_contact.errors.count, "error") %> prohibited this related_contact from being saved:</h2>

      <ul>
      <% @related_contact.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :contact_ids %><br>
    <%= f.text_area :contact_ids %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
