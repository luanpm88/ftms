                <div class="grid simple ">
                  <div class="grid-body no-border filters seminar-filter">
                  <div class="row order-filters">
                      
                      <div class="col-md-4">
                        <h4>Program</h4>
						<%= select_tag 'filter[course_types[]]', options_for_select(CourseType.active_course_types.collect{ |u| [u.short_name, u.id] }), class: "modern_select width100 multiple", multiple: "multiple" %>
                      </div>
					  
					  <div class="col-md-3">
							<h4>Status</h4>							
							<%= select_tag 'status', options_for_select(Seminar.status_options, params[:status]), class: "modern_select width100" %>
						</div>
                      
                      <div class="col-md-3 student-seminar-filter">
							<h4>Student</h4>
							<div class="">
								<input style="width:100%;margin-bottom: 5px;" name="contact_id" data="<%= contacts_path(is_individual: true,format: "json") %>" class="select2-ajax-contacts" value="<%= @student.id if !@student.nil? %>" text="<%= @student.name if !@student.nil? %>" />
							</div>
						</div>
                      
                      
                  </div>
                  </div>		    
				</div>