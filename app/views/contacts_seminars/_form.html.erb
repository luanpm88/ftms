<%= form_for(@contacts_seminar) do |f| %>
  <% if @contacts_seminar.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contacts_seminar.errors.count, "error") %> prohibited this contacts_seminar from being saved:</h2>

      <ul>
      <% @contacts_seminar.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :contact_id %><br>
    <%= f.number_field :contact_id %>
  </div>
  <div class="field">
    <%= f.label :seminar_id %><br>
    <%= f.number_field :seminar_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
