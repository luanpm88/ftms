<%= form_for(@course_types_subject) do |f| %>
  <% if @course_types_subject.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@course_types_subject.errors.count, "error") %> prohibited this course_types_subject from being saved:</h2>

      <ul>
      <% @course_types_subject.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :course_type_id %><br>
    <%= f.number_field :course_type_id %>
  </div>
  <div class="field">
    <%= f.label :subject_id %><br>
    <%= f.number_field :subject_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
