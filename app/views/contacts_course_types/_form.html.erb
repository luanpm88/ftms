<%= form_for(@contacts_course_type) do |f| %>
  <% if @contacts_course_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contacts_course_type.errors.count, "error") %> prohibited this contacts_course_type from being saved:</h2>

      <ul>
      <% @contacts_course_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :contact_id %><br>
    <%= f.number_field :contact_id %>
  </div>
  <div class="field">
    <%= f.label :course_type_id %><br>
    <%= f.number_field :course_type_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
