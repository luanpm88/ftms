<%= form_for(@contact_tag) do |f| %>
  <% if @contact_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contact_tag.errors.count, "error") %> prohibited this contact_tag from being saved:</h2>

      <ul>
      <% @contact_tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
