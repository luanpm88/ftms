<%= form_for(@course_price) do |f| %>
  <% if @course_price.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@course_price.errors.count, "error") %> prohibited this course_price from being saved:</h2>

      <ul>
      <% @course_price.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :course_id %><br>
    <%= f.number_field :course_id %>
  </div>
  <div class="field">
    <%= f.label :prices %><br>
    <%= f.text_area :prices %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
