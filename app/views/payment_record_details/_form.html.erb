<%= form_for(@payment_record_detail) do |f| %>
  <% if @payment_record_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@payment_record_detail.errors.count, "error") %> prohibited this payment_record_detail from being saved:</h2>

      <ul>
      <% @payment_record_detail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :contacts_course_id %><br>
    <%= f.number_field :contacts_course_id %>
  </div>
  <div class="field">
    <%= f.label :books_contact_id %><br>
    <%= f.text_field :books_contact_id %>
  </div>
  <div class="field">
    <%= f.label :amount %><br>
    <%= f.text_field :amount %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
